{
  "name": "root-package",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client",
    "start": "npm run start --prefix server && npm run build --prefix client",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"

  }
}
